/** ======================= PATIENT TYPES ======================= */

interface Patient {
    firstName: string
    lastName: string
    cin: string
    phoneNumber: string
    dateOfBirth: string
    createdAt: string
}

export type { Patient }
